Place the provided image (the plaza photo) here so the site uses it for the header background.

Save the image with this filename:

assets/header-bg.jpg

If your image is PNG use `assets/header-bg.png` and update `css/styles.css` background-image accordingly.

To preview locally, run:

```powershell
python -m http.server 8000
# then open http://localhost:8000/
```
